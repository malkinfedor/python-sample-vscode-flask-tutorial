trigger:
- master

pool:
  vmImage: 'Ubuntu-16.04'

variables:
  imageName: 'malkinfedor/pipelines-python-docker:$(Build.BuildId)'
  dockerHub: 'docker-registry-susin'
  tag: 'test'

steps:
- task: Docker@2
  displayName: Build an image
  inputs:
    repository: $(imageName)
    imageName: $(imageName)
    command: build
    Dockerfile: ./Dockerfile
    #tags: |
    #  test1
    #  test2
    
#- task: Docker@2
#  displayName: Login to Docker Hub
#  inputs:
#    command: login
#    containerRegistry: $(dockerHub)

- script: |
    pwd
    ls -l
    whoami
    docker image ls
  displayName: 'get info about dir and user'
    
- task: Docker@2
  displayName: Push image
  inputs:
    containerRegistry: |
      $(dockerHub)
    repository: $(imageName)
    #imageName: $(imageName)
    command: push
    #tags: |
    #  $(Build.BuildId)
    #  test1
    #  test2
    
- script: |
    pwd
    ls -l
    whoami
    docker image ls
  displayName: 'get info about dir and user'
